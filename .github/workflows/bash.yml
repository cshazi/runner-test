name: Bash test

on:
  workflow_dispatch:

jobs:
  run_tests:
    runs-on: [self-hosted, Linux, X64]
    steps:
    - name: Run script file
      shell: bash
      run: |
         echo hello world
         lscpu
